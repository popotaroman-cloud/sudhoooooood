# stu1 — ระบบโครงสร้างพื้นฐาน ยืนยันตัวตน และ IT Admin

**Modules รับผิดชอบ:** `shared-kernel` · `auth` · `it-admin`  
**Infrastructure:** Traefik · OPA · BPMN Engine · Docker · CI/CD  
**Phase:** 0 (เริ่มก่อนทุกคน — unblocks stu2, stu3, stu4)  
**FR รวม:** 20 FRs (FR-SK × 6, FR-AUTH × 7, FR-IT × 7)  
**อ้างอิง:** [fr/shared-kernel.md](fr/shared-kernel.md) · [fr/auth.md](fr/auth.md) · [fr/it-admin.md](fr/it-admin.md)

---

## 1. ชื่อโครงงาน

**ระบบโครงสร้างพื้นฐานและการจัดการตัวตนสำหรับระบบบริหารงานคณะ**  
*(Faculty Administration Platform — Infrastructure & Identity Layer)*

ทำหน้าที่เป็น **รากฐานของระบบทั้งหมด** ทุก module จำเป็นต้องรอ deliverable จาก stu1 ก่อนจึงจะเริ่มพัฒนาได้

---

## 2. ฟังก์ชันหลัก

| # | ฟังก์ชัน | รายละเอียด |
|---|---|---|
| 1 | Single Sign-On (SSO) | Login ครั้งเดียว ใช้ได้ทุก module ผ่าน Keycloak + OIDC |
| 2 | JWT & Token Management | ออก / refresh / revoke token, forward claims ผ่าน Traefik |
| 3 | API Gateway | Route, TLS termination, rate limit, circuit breaker ด้วย Traefik |
| 4 | Authorization Policy | กำหนด RBAC/ABAC และ row-level filter ด้วย OPA + Rego |
| 5 | Shared Master Data | User, Department, AcademicUnit, FiscalYear, Role — read-only API ให้ทุก module |
| 6 | Account Provisioning | สร้าง/ปิด account เมื่อพนักงานเริ่ม/สิ้นสุดการทำงาน |
| 7 | IT Helpdesk | แจ้งปัญหา IT, SLA tracking, hardware/software inventory |
| 8 | CI/CD Pipeline | automated build, test, lint, container image push |

---

## 3. เปรียบเทียบซอฟต์แวร์ที่มีอยู่

### 3.1 Identity & Access Management

| ซอฟต์แวร์ | ลักษณะ | ข้อดี | ข้อจำกัด | เหตุผลที่ไม่เลือก |
|---|---|---|---|---|
| **Keycloak** (เลือก) | Open-source IdP | OIDC/SAML, LDAP sync, ฟรี | ต้อง host เอง | — |
| Auth0 | SaaS IdP | ง่าย, managed | ค่าใช้จ่ายสูง, vendor lock | งบจำกัด |
| Microsoft Entra ID | Cloud IdP | integrate กับ M365 | ต้อง Microsoft ecosystem | ไม่ยืดหยุ่น |
| LDAP standalone | Directory | lightweight | ไม่มี OIDC, UI น้อย | ไม่ครบ feature |

### 3.2 API Gateway

| ซอฟต์แวร์ | ลักษณะ | ข้อดี | ข้อจำกัด | เหตุผลที่ไม่เลือก |
|---|---|---|---|---|
| **Traefik v3** (เลือก) | Reverse proxy / gateway | dynamic config, Let's Encrypt auto, forward-auth | ต้อง learn config | — |
| Nginx | Web server / proxy | เสถียร, เอกสารเยอะ | config static, ไม่มี circuit breaker | ยืดหยุ่นน้อยกว่า |
| Kong | API Gateway | plugin ecosystem | ซับซ้อน, license tier | เกิน scope |
| AWS API Gateway | SaaS | managed | vendor lock, ราคา | ต้อง cloud |

### 3.3 Policy Engine

| ซอฟต์แวร์ | ลักษณะ | ข้อจำกัด | เหตุผลที่ไม่เลือก |
|---|---|---|---|
| **OPA** (เลือก) | Policy-as-code, Rego | ต้องเรียน Rego | — |
| Casbin | RBAC library | hard-code ใน app | ไม่แยก policy layer |
| AWS IAM | Cloud IAM | vendor lock | ไม่เหมาะ on-premise |

---

## 4. Dev Tools & Tech

| หมวด | เครื่องมือ / เทคโนโลยี |
|---|---|
| **Container** | Docker Desktop, Docker Compose |
| **API Gateway** | Traefik v3 |
| **Auth** | Keycloak 24+, LDAP (OpenLDAP สำหรับ dev) |
| **Policy** | OPA (Open Policy Agent), Rego language |
| **Database** | PostgreSQL 16 (`shared` schema), pgAdmin |
| **Cache** | Redis 7 |
| **CI/CD** | GitHub Actions (lint → test → build → push image) |
| **Container Registry** | GitHub Container Registry (ghcr.io) |
| **Backend Framework** | NestJS (TypeScript) หรือ Spring Boot |
| **Testing** | Jest / JUnit, OPA conftest (policy unit test) |
| **Diagram** | PlantUML, draw.io |
| **Secret Mgmt** | Docker secrets / `.env` (dev), Vault (prod) |
| **Monitoring** | Prometheus + Grafana (ตั้ง dashboard พื้นฐาน) |

---

## Modules & Function Subtasks

### Module: shared-kernel

| Subtask | รายละเอียด | Priority |
|---|---|:---:|
| SK-1 | ออกแบบ schema `shared` ใน PostgreSQL | P0 |
| SK-2 | สร้าง migration: users, departments, academic_units, fiscal_years, roles | P0 |
| SK-3 | Seed data: ภาควิชา, สาขาวิชา, role definitions | P0 |
| SK-4 | Internal API: `GET /internal/shared/users/{id}` | P0 |
| SK-5 | Internal API: `GET /internal/shared/departments/{id}` | P0 |
| SK-6 | Internal API: `GET /internal/shared/academic-units` (filter by type) | P0 |
| SK-7 | Internal API: `GET /internal/shared/fiscal-years/current` | P0 |
| SK-8 | Redis cache layer (5 min TTL, invalidate on write) | P1 |
| SK-9 | Keycloak webhook → sync user เข้า shared.users | P1 |

### Module: auth

| Subtask | รายละเอียด | Priority |
|---|---|:---:|
| AUTH-1 | ติดตั้งและ configure Keycloak (realm, client, LDAP) | P0 |
| AUTH-2 | Traefik Forward Auth middleware (`/auth/verify` endpoint) | P0 |
| AUTH-3 | JWT validation + extract claims (user_id, roles, dept) | P0 |
| AUTH-4 | Forward headers: `X-User-Id`, `X-User-Roles`, `X-Department-Id` | P0 |
| AUTH-5 | Role assignment UI ใน Keycloak (กำหนด roles ตาม FR-SK-004) | P1 |
| AUTH-6 | MFA setup (TOTP) สำหรับ sensitive roles | P1 |
| AUTH-7 | Logout + token blacklist (Redis) | P1 |
| AUTH-8 | Audit log: login/logout/failed events | P2 |

### Module: it-admin

| Subtask | รายละเอียด | Priority |
|---|---|:---:|
| IT-1 | Account provisioning — subscribe `StaffOnboardedEvent` | P1 |
| IT-2 | Account deprovisioning — subscribe `StaffOffboardedEvent` | P1 |
| IT-3 | Permission request CRUD + approval workflow | P2 |
| IT-4 | Hardware asset management (CRUD + QR) | P2 |
| IT-5 | Software license tracking + expiry alert | P2 |
| IT-6 | Helpdesk ticket system (CRUD, priority, SLA timer) | P2 |
| IT-7 | System health dashboard (Prometheus metrics pull) | P3 |

### Infrastructure

| Subtask | รายละเอียด | Priority |
|---|---|:---:|
| INF-1 | `docker-compose.yml` ครบทุก service (dev environment) | P0 |
| INF-2 | Traefik config: routing rules, TLS, middleware chain | P0 |
| INF-3 | OPA setup + base Rego policy (allow all → จะ refine ทีหลัง) | P0 |
| INF-4 | BPMN Engine (Camunda 8 / Flowable) container + basic config | P0 |
| INF-5 | GitHub Actions: lint + test pipeline | P1 |
| INF-6 | GitHub Actions: Docker build + push to ghcr.io | P1 |
| INF-7 | ArchUnit / Deptrac rule สำหรับ enforce module boundary | P1 |
| INF-8 | Prometheus + Grafana stack (basic dashboard) | P2 |
| INF-9 | เขียน `README.md` วิธีรัน local dev สำหรับ stu2–4 | P0 |

---

## API Contract — Interface ที่ stu1 กำหนด (สัปดาห์ 1–2)

stu2, stu3, stu4 implement `MockSharedKernelService` จาก interface นี้ก่อนที่ stu1 จะเสร็จ

```typescript
// contracts/shared.interface.ts
export interface ISharedKernelService {
  getUserById(id: string): Promise<UserDTO>
  getDepartmentById(id: string): Promise<DepartmentDTO>
  getAcademicUnitById(id: string): Promise<AcademicUnitDTO>
  getAcademicUnitsByType(type: 'faculty'|'department'|'program'): Promise<AcademicUnitDTO[]>
  getCurrentFiscalYear(): Promise<FiscalYearDTO>
}

// JWT forward headers — Traefik inject ให้ทุก request
// X-User-Id: string
// X-User-Roles: string (comma-separated)
// X-Department-Id: string
```

---

## Task Planning (Parallel)

### สัปดาห์ 1–2 — Contract + Design (ทุกคนทำพร้อมกัน)

| งาน | รายละเอียด |
|---|---|
| ประชุมทีม | ตกลง API contracts ทุกตัวที่ข้าม student |
| Commit contracts | push `contracts/*.interface.ts` + `contracts/*.dto.ts` ลง repo |
| Schema design | ออกแบบ `shared` schema + migration script |
| INF-1 skeleton | สร้าง `docker-compose.yml` skeleton (service list ก่อน config ทีหลัง) |

---

### สัปดาห์ 3–4 — **[M1] Infra + Shared Kernel** ← *unblocks stu2,3,4*

| สัปดาห์ | งาน | Parallel กับ |
|---|---|---|
| 3 | INF-1,2,3,4 (docker-compose, Traefik, OPA, BPMN) · SK-1,2,3 (schema+seed) · INF-9 (README) | stu2,3,4 ทำ schema design + mock setup |
| 4 | AUTH-1,2 (Keycloak+Forward Auth) · SK-4→7 (internal API ครบ) | stu2,3,4 เริ่ม core module ด้วย mock |

**🔴 M1 Deliverable (end of W3):** `docker-compose up` → ทุก service พร้อม + README ครบ  
**🔴 M2 Deliverable (end of W5):** Shared Kernel API + JWT headers → stu2,3,4 switch mock → real

---

### สัปดาห์ 5–6 — Auth ครบ + CI/CD

| สัปดาห์ | งาน | Parallel กับ |
|---|---|---|
| 5 | AUTH-3,4 (JWT+headers) · AUTH-5,6 (role+MFA) · SK-8,9 (Redis+webhook) | stu2 HR core, stu3 Finance core, stu4 Academic core |
| 6 | INF-5,6 (CI/CD pipeline) · INF-7 (ArchUnit) | stu2,3,4 ยังคง core module |

---

### สัปดาห์ 7–10 — IT Admin

| สัปดาห์ | งาน | Parallel กับ |
|---|---|---|
| 7 | IT-1,2 (provisioning/deprovisioning) | stu2 HR adv, stu3 Budget BPMN, stu4 Academic timetable |
| 8 | IT-3,4 (permission + hardware) | stu2 Document, stu3 Finance reports, stu4 Enrollment |
| 9 | IT-5,6 (license + helpdesk) | stu2 Facility, stu3 Procurement, stu4 Research |
| 10 | IT-7 (health dashboard) · AUTH-7,8 (logout+audit) | stu2 Facility adv, stu3 Proc adv, stu4 Research adv |

---

### สัปดาห์ 11–12 — OPA Policies (ร่วมกับทุกคน)

| สัปดาห์ | งาน | ร่วมกับ |
|---|---|---|
| 11 | `hr.rego` (salary masking) · `finance.rego` (approval tier) | stu2 review hr.rego · stu3 review finance.rego |
| 12 | `academic.rego` · `research.rego` · `data-masking.rego` | stu4 review ทั้งคู่ |

---

### สัปดาห์ 13–14 — OPA Unit Test + Monitoring

| สัปดาห์ | งาน | Parallel กับ |
|---|---|---|
| 13 | OPA conftest unit test ทุก policy · INF-8 (Grafana) | stu2,3,4 full integration test |
| 14 | Row-level filter + data masking test ครบ | stu2,3,4 bug fix |

---

### สัปดาห์ 15–16 — Load Test + Demo

| สัปดาห์ | งาน |
|---|---|
| 15 | k6 load test · security checklist (OWASP) · deployment guide |
| 16 | Demo prep · rehearsal · final doc |

---

## Sync Milestones (ภาพรวมทีม)

| # | สัปดาห์ | ผู้ส่ง | สิ่งที่ส่ง | ผู้รับ switch จาก |
|---|:---:|---|---|---|
| M1 | 3 | **stu1** | `docker-compose up` + README | Mock infra → real infra |
| M2 | 5 | **stu1** | Shared Kernel API + JWT headers | Mock SK → real SK |
| M3 | 6 | **stu2** | `GET /internal/hr/employees/{id}` | Mock HR → real HR |
| M4 | 9 | **stu3** | `GET /internal/finance/budget-balance/{id}` | Mock Finance → real Finance |
| M5 | 12 | **stu2** | `GET /internal/facility/rooms/{id}/availability` | Mock Facility → real Facility |
| M6 | 13 | **ทุกคน** | Full integration — swap mock ทั้งหมด | — |

> **กฎ:** ถ้า milestone ไม่ผ่านตรงเวลา → ผู้รับใช้ Mock ต่อ ไม่หยุดรอ
